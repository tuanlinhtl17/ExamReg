<div class="modal" id="suaHocPhan">
  <div class="modal-dialog">
    <div class="modal-content">

      <!-- Modal Header -->
      <div class="modal-header">
        <h4 class="modal-title">Sửa thông tin học phần</h4>
        <button type="button" class="close" data-dismiss="modal">&times;</button>
      </div>

      <!-- Modal body -->
      <div class="modal-body">
        <%= form_for [:admin, hoc_phan], remote: true do |f| %>
          <div class="register-top-grid">
            <div class="mation">
              <span>Tên học phần<label>*</label></span>
              <%= f.text_field :ten, class: "form-control" %>
            </div>
            <br>
            <div class="mation">
              <span>Số tín chỉ<label>*</label></span>
              <%= f.text_field :so_tin_chi, class: "form-control" %>
            </div>
            <br></br>
            <div class="modal-footer">
              <%= f.submit "Xác nhận", class: "btn btn-primary" %>
              <button type="button" class="btn btn-danger" data-dismiss="modal">Đóng</button>
            </div>
          </div>
        <% end %>
        <div class="clearfix"> </div>
      </div>
    </div>
  </div>
</div>
