<header>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="#"><%= image_tag "ABCUni.png", alt: "ABCUni", id: "logo" %></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <span id="web-title">CỔNG THÔNG TIN ĐĂNG KÝ DỰ THI</span>
      </ul>
      <% if logged_in? %>
        <ul class="navbar-nav pull-right">
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              Chào mừng: <strong>Nguyễn Tuấn Linh</strong>
            </a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
              <% if is_admin? %>
                <%= link_to "Đăng xuất", destroy_admin_session_path, method: "delete", class: "dropdown-item"  %>
              <% else %>
                <%= link_to "Đăng xuất", destroy_sinh_vien_session_path, method: "delete", class: "dropdown-item" %>  
              <% end %>
            </div>
          </li>
        </ul>
      <% end %>
    </div>
  </nav>
</header>
